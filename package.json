{
  "name": "cloudflare-monitor",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:web": "npm --prefix web run build",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate": "wrangler d1 migrations apply cloudflare_monitor_db"
  },
  "devDependencies": {
    "wrangler": "^4.52.0"
  }
}
